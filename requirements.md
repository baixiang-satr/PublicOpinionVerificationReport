# 舆情验证报告工具需求文档

## 1. 文档信息

- 项目名称：舆情验证报告工具（Public Opinion Verification Report Tool）
- 文档版本：v0.4
- 编写日期：2026-07-28
- 需求基线：以 `template/` 目录和其中的 `template.xlsx` 为唯一交付模板

## 2. 结论与约束

本项目不是生成一份新的通用爬虫报告，而是将批量抓取结果填入一份固定的交付模板。`template/` 是只读的基准目录，源文件及其中的示例数据、截图均不得被修改、删除或重命名。

每次任务必须在独立临时目录中复制该目录，清除副本中既有业务数据后写入本次结果；最终仅交付 `template.zip`。压缩包中的目录结构、工作表、列名、下拉选项、单元格格式、保护状态和截图/附件的平铺放置方式必须与基准模板一致。

已核对 `template.xlsx`：它可由 Microsoft Excel 打开，但文件使用 Office OLE 容器且工作表受保护，不是可由 `openpyxl` 安全读写并保留全部特性的普通 OpenXML 工作簿。因此，模板副本的读写必须采用能够保留 Excel 原生格式、数据验证和保护的 Windows Excel 自动化方案；不得重建工作簿、另造列或用普通库覆盖保存。

## 3. 产品目标

用户导入一批网页 URL 后，工具自动抓取页面基础信息、生成内容页/作者主页取证截图并按平台归类，最终产生可被下游系统接收的 `template.zip`。

每个可交付记录至少应能对应来源 URL、平台标准值、已取得的标题或正文和主截图；作者/账号、发布时间及作者主页截图尽力采集，无法取得时保留空白。单条页面失败时，任务继续执行，运行态保留状态码和错误原因。

## 4. 交付物契约

### 4.1 不可变基准

- `template/` 仅作为源模板，程序不得在其中写入任务结果。
- 程序启动任务前应复制 `template/` 至任务临时目录中的 `template/`。
- 写入前，副本中第 3 行及之后的历史业务记录必须清空；第 1 行字段名和第 2 行示例/提示必须保留。清空操作不得删除行、列、工作表、数据验证、单元格样式、公式、批注或保护设置。
- 本次实际生成的截图与下载附件应替换副本中的历史图片；不得将与本次 Excel 记录无关的基准示例图片混入交付包。

### 4.2 ZIP 结构

最终文件名固定为 `template.zip`。默认压缩包结构如下，根目录名称为 `template/`：

```text
template.zip
└── template/
    ├── template.xlsx
    ├── 001.jpg
    ├── 001主页.jpg
    ├── 001_01.jpg
    └── ...
```

- `template.xlsx` 必须是从基准模板复制并填充后的副本，文件名不得变更。
- 截图和附件与 `template.xlsx` 处于同一目录，不得引入自定义子目录。
- ZIP 内不得额外写入调试报告、JSON、日志或浏览器缓存。运行日志可在本机任务目录保留，但不属于交付包。
- 打包前必须校验工作簿中所有非空的截图/附件文件名均能在 ZIP 的 `template/` 根目录找到同名文件。

## 5. 模板结构与路由

模板包含以下 8 个工作表，名称和顺序必须保持不变。

| 工作表 | URL 抓取适用性 | 自动路由的典型平台 | 主要交付字段 |
| --- | --- | --- | --- |
| 电商平台 | 适用 | 拼多多、天猫、淘宝、闲鱼、1688、京东、抖音电商 | 商品 URL、标题、店铺、处置内容、商品截图 |
| 公众号 | 适用 | 微信公众号、百度百家号 | 文章链接、公众号账号/UIN/名称、文章内容、文章截图 |
| 群聊 | 不适用 | 微信群、QQ群 | 群号、群名、成员信息、群聊截图 |
| 朋友圈 | 不适用 | 微信朋友圈 | 用户账号、发布时间、内容、朋友圈截图 |
| 图文视频 | 适用 | 快手、小红书、抖音、哔哩哔哩、微信视频、搜狐视频、土豆、优酷、西瓜、爱奇艺 | URL、账号、昵称、文本类型、账号截图 |
| 微博博客 | 适用 | 微博、贴吧、知乎 | URL、昵称、文本类型、网页截图 |
| 生活资讯 | 适用 | 今日头条、网易新闻、凤凰新闻、搜狐新闻、狐友、虎扑、美团、懂车帝 | URL、昵称、账号、文本类型、网页截图 |
| 浏览器 | 适用 | UC、360、华为、QQ 浏览器 | URL、账号、昵称、文本类型、账号截图 |

群聊和朋友圈没有 URL 列，不能由“批量网页 URL 抓取”自动生成记录。MVP 对这两类输入应标记为“不适用，需人工采集/补录”，保持工作表为空；不得为了容纳网页数据而把它们错误写入这些表。

对于无法确定平台或页面类型的 URL，程序不得猜测或填入不存在的“其他”值。应在界面中显示“未匹配模板平台”，记录运行错误，并允许用户选择一个模板允许的平台后重试。

## 6. 标准输入与字段规则

### 6.1 通用规则

- 标题中带“`(必填)`”的列，在写入前必须校验非空；因抓取失败无法满足时，不得伪造值，须将该记录留在运行态失败列表。
- 所有“发布平台”与“文本类型”必须严格使用模板下拉枚举中的完整字符串，不得缩写、翻译或自行新增。
- 模板的日期单元格实际格式为 `yyyy/mm/dd hh:mm:ss`，而表头说明写作 `yyyy-mm-dd HH:mm:ss`。程序应接受两种使用英文半角分隔符的输入，写入 Excel 原生日期时间值，并保留模板已有的显示格式；不得写入本地化文字日期或相对时间。
- “其他附件文件名(多个逗号分隔)”和“其他文件名(多个逗号分隔)”只能写入文件基名，使用英文逗号 `,` 分隔，不得包含目录、反斜杠、全角逗号或无扩展名的名称。
- 所有附件文件名必须真实存在于 ZIP 内的 `template/` 根目录，扩展名应与实际 MIME 类型一致。
- 截图名称和附件名称必须仅包含 Windows 安全文件名字符；同一 ZIP 内不可重名。

### 6.2 平台与文本类型枚举

| 工作表 | 发布平台允许值 | 其他枚举 |
| --- | --- | --- |
| 电商平台 | `拼多多`、`阿里_天猫_电商平台`、`阿里_淘宝_电商平台`、`阿里_闲鱼_电商平台`、`阿里_1688_电商平台`、`京东_京东商城_电商平台`、`字节跳动_抖音_电商平台` | 处置对象：`商家`、`评论回复` |
| 公众号 | `微信-公众号`、`百度_百家号_公众号` | 处置对象：`正文`、`评论回复` |
| 群聊 | `微信-群聊`、`QQ-群聊` | 无 |
| 朋友圈 | `微信-朋友圈` | 无 |
| 图文视频 | `快手科技_快手_图文视频`、`行吟科技_小红书_图文视频`、`字节跳动_抖音_图文视频`、`幻电科技_哔哩哔哩_图文视频`、`腾讯_微信_图文视频`、`搜狐_搜狐视频_图文视频`、`阿里巴巴_土豆_图文视频`、`阿里巴巴_优酷_图文视频`、`字节跳动_西瓜视频_图文视频`、`爱奇艺_爱奇艺_图文视频` | 文本类型：`正文`、`评论回复` |
| 微博博客 | `新浪_新浪微博_博客贴吧`、`百度_百度贴吧_博客贴吧`、`知乎_知乎_博客贴吧` | 文本类型：`正文`、`评论回复` |
| 生活资讯 | `字节跳动_今日头条_生活资讯`、`网易_网易新闻_生活资讯`、`凤凰网_凤凰新闻_生活资讯`、`搜狐_搜狐新闻_生活资讯`、`搜狐_狐友_生活资讯`、`虎扑_虎扑_生活资讯`、`三快_美团_生活资讯`、`字节跳动_懂车帝_生活资讯` | 文本类型：`正文`、`评论回复` |
| 浏览器 | `阿里巴巴_UC浏览器_浏览器`、`360_360浏览器_浏览器`、`华为_华为浏览器_浏览器`、`腾讯_QQ浏览器_浏览器` | 文本类型：`正文`、`评论回复` |

### 6.3 各表字段与抓取映射

#### 电商平台

| 列 | 模板字段 | 写入来源 |
| --- | --- | --- |
| A | 商品URL(必填) | 原始或最终商品 URL |
| B | 发布平台(必填) | 域名和页面类型匹配后的标准枚举 |
| C | 商品标题(必填) | 页面标题/商品标题 |
| D | 处置对象(必填) | 页面为商品主体填 `商家`；评论页填 `评论回复` |
| E | 处置内容(必填) | 商品描述或评论正文的清洗摘要 |
| F | 店铺名称(必填) | 店铺/商家展示名称 |
| G | 商品截图文件名(必填) | 页面主截图文件名 |
| H | 其他附件文件名 | 作者/店铺主页截图文件名 |

#### 公众号

| 列 | 模板字段 | 写入来源 |
| --- | --- | --- |
| A | 文章链接(必填) | 原始或最终文章 URL |
| B | 发布平台(必填) | `微信-公众号` 或 `百度_百家号_公众号` |
| C | 文章标题(必填) | 页面标题 |
| D | 公众号微信号(必填) | 优先写抓取到的账号 ID；公开页确实不提供 ID 时可用昵称回退，并记录审计标记 |
| E | 公众号UIN | 抓取到的 UIN |
| F | 公众号名称 | 账号展示名称 |
| G | 处置对象(必填) | `正文` 或 `评论回复` |
| H | 信息内容(必填) | 文章正文或评论正文的清洗摘要 |
| I | 发布时间 | 标准化后的发布时间 |
| J | 文章截图文件名(必填) | 页面主截图文件名 |
| K | 其他附件文件名 | 账号主页截图文件名 |

#### 图文视频

| 列 | 模板字段 | 写入来源 |
| --- | --- | --- |
| A | URL | 页面 URL；仅 `腾讯_微信_图文视频` 允许为空，但 URL 抓取任务中应保留抓取 URL |
| B | 用户账号(必填) | 平台用户 ID/唯一账号；无可靠 ID 时应失败或人工补录 |
| C | 昵称(必填) | 作者展示昵称 |
| D | 发布平台(必填) | 模板允许的平台枚举 |
| E | 文本类型(必填) | `正文` 或 `评论回复` |
| F | 发布时间 | 标准化后的发布时间 |
| G | 信息内容(必填) | 视频文案、图文正文或评论正文的清洗摘要 |
| H | 账号截图名(必填) | 页面主截图文件名，按模板原列名填写 |
| I | 其他文件名 | 作者主页截图文件名 |

#### 微博博客

| 列 | 模板字段 | 写入来源 |
| --- | --- | --- |
| A | URL(必填) | 原始或最终页面 URL |
| B | 昵称(必填) | 作者展示昵称 |
| C | 发布平台(必填) | 模板允许的平台枚举 |
| D | 文本类型(必填) | `正文` 或 `评论回复` |
| E | 发布时间 | 标准化后的发布时间 |
| F | 信息内容(必填) | 博文、帖子、问答或评论正文的清洗摘要 |
| G | 网页截图名(必填) | 页面主截图文件名 |
| H | 其他文件名 | 作者主页截图文件名 |

#### 生活资讯

| 列 | 模板字段 | 写入来源 |
| --- | --- | --- |
| A | URL(必填) | 原始或最终页面 URL |
| B | 昵称(必填) | 作者展示昵称 |
| C | 发布平台(必填) | 模板允许的平台枚举 |
| D | 文本类型(必填) | `正文` 或 `评论回复` |
| E | 用户账号 | 平台用户 ID |
| F | 发布时间 | 标准化后的发布时间 |
| G | 信息内容(必填) | 正文或评论正文的清洗摘要 |
| H | 网页截图名(必填) | 页面主截图文件名 |
| I | 其他文件名 | 作者主页截图文件名 |

#### 浏览器

| 列 | 模板字段 | 写入来源 |
| --- | --- | --- |
| A | URL(必填) | 原始或最终页面 URL |
| B | 用户账号(必填) | 平台用户 ID/唯一账号 |
| C | 昵称(必填) | 作者展示昵称 |
| D | 发布平台(必填) | 模板允许的平台枚举 |
| E | 文本类型(必填) | `正文` 或 `评论回复` |
| F | 发布时间 | 标准化后的发布时间 |
| G | 信息内容(必填) | 正文或评论正文的清洗摘要 |
| H | 账号截图名(必填) | 页面主截图文件名 |
| I | 其他文件名 | 作者主页截图文件名 |

群聊和朋友圈表的列不具备 URL 爬虫映射，保留给未来人工采集导入流程。不得因其存在于模板中而要求 URL 爬虫模拟、登录或抓取私密群聊/朋友圈内容。

## 7. 批量抓取需求

### FR-001 输入与任务创建

- 支持 TXT、CSV 和标准 OpenXML `.xlsx` 中的 URL 导入；从文本或指定列抽取 `http://`、`https://` URL。
- 保留输入首次出现顺序，完成 URL 规范化和去重；原始 URL、规范化 URL 和最终重定向 URL 均保存为运行态字段。
- 创建任务前展示 URL 数量、可自动路由数量、未匹配平台数量和重复项数量。
- 不得把 `template.xlsx` 当作输入清单或用普通 `.xlsx` 库覆盖保存。

### FR-002 页面访问与基础信息

对每个 URL，系统必须使用可执行 JavaScript 的浏览器上下文访问页面，并采集：

- 页面标题、清洗后的正文全文和适合模板 `信息内容/处置内容` 的正文摘要；
- 作者昵称、账号 ID、作者主页 URL、店铺/公众号信息；
- 发布时间及其原始字符串；
- 页面内候选图片 URL 列表（去重、过滤非 `http(s)`、过滤明显装饰图）；
- 原始 URL、最终 URL、HTTP 状态码、重定向链、抓取开始/完成时间、耗时、抓取状态和错误信息。

提取优先级为平台专用 DOM、JSON-LD、Open Graph/meta、通用 DOM、可审计的页面可见文本。不得仅凭 URL 字符串臆造作者、发布时间、账号 ID 或文本类型。

“正文摘要”应是清理导航、广告和重复文本后的可读内容。长度上限必须可配置，并不得超过 Excel 单元格 32,767 字符限制；截断时记录运行态标记。

解析和截图前必须调用统一访问诊断工具，识别登录墙、验证码页、JavaScript 未渲染提示、JSON/API 响应、内容不存在、内容链接跳回首页和空白页。受限页不得作为主截图写入模板。可视模式下，登录和验证码允许用户在有界等待时间内手工处理；系统不得自动破解或隐匿自动化身份。

### FR-003 平台识别与记录路由

- 系统应根据最终域名、页面元数据和平台专用规则识别“工作表 + 发布平台枚举 + 正文/评论回复”。
- 同一 URL 必须只进入一个工作表；同一任务中的全局抓取序号保持稳定，即使结果被分散写入不同工作表。
- 平台规则须版本化配置，允许维护人员新增域名和解析器，但不得直接在模板中新增下拉值。
- 对账号 ID、公众号微信号等模板必填字段，应优先提取公开页面中的真实 ID。页面确实不提供 ID 但能可靠提取昵称时，允许用昵称回退，并在 `author_id_is_fallback` 和字段来源中标记 `nickname_fallback`；昵称也缺失时标记“待人工补录”。

### FR-004 页面图片与附件

- 系统保存页面图片 URL 列表作为运行态数据，但正文图片不作为交付附件。
- 仅当 DOM 正文为空且启用 OCR 时，按数量/大小上限临时下载公开图片；识别完成后立即删除。
- 下载失败、跨域拦截、非图片响应或超限项只记入运行态错误，不得在 Excel 或 ZIP 中留下文件名。

### FR-005 截图

- 每条成功访问且取得标题或正文的 URL 必须生成一张页面主截图，使用全局证据编号 `{序号:03d}.{jpg|png}`，例如 `001.jpg`。
- 提取到作者主页 URL 时，应访问该主页并生成 `{序号:03d}主页.{jpg|png}`，例如 `001主页.jpg`；该文件名应追加到对应行的附件列表。
- 页面主截图应填入模板指定的截图列；作者主页截图没有独立列，必须放入“其他附件/其他文件名”列。
- 截图扩展名必须与实际编码一致。截图前等待页面主要内容稳定，避免空白页、错误页或加载遮罩被当作成功取证。
- 无作者主页、主页需要登录、主页抓取失败时不得阻断页面主记录；应保留主截图，并在运行态记录对应原因。
- 不得以 Excel 行号作为图片编号。编号应是本任务的稳定全局证据 ID，以便分表后仍能一一关联。

### FR-006 模板填充

- 程序必须复制而非移动或修改源 `template/`。
- 仅向副本的可填写业务区域写入值。不得新增、删除、隐藏、重命名或调整任何工作表、列、表头、下拉值、格式、保护、合并单元格、行高或列宽。
- 数据行按各表路由后的原始输入顺序写入；同一平台内保持输入顺序。
- Excel 中引用的每个附件名都必须能在交付目录找到；无附件时保持空单元格。
- 模板当前视觉格式主要覆盖第 3 至 201 行，电商平台表的已用区域仅到第 4 行。程序在写入前必须检测模板中可安全填写的行数；超过容量时应停止导出并明确提示，不得自行插入未获批准的行。
- 写入并保存后必须使用 Excel 打开校验，确保无修复提示、工作表保护与数据验证仍在、表头和工作表顺序不变。

### FR-007 状态码与错误信息

状态码、重定向信息、耗时、解析字段来源和错误信息是必采字段，但当前模板没有承载它们的列，也禁止新增列。因此它们必须保存在任务运行态结果、GUI 结果列表和本机日志中，不能写入 `template.zip`。

单条失败不得导致批任务失败。任务结束时 GUI 必须显示成功数、待人工补录数、未匹配平台数、失败数，以及可定位到原始 URL 的错误信息。

### FR-008 GUI 与运行控制

- 提供 URL 文件选择/拖拽、并发数、超时、重试次数、截图格式、无头模式和可选 Cookie/登录态配置。
- 支持开始、取消及失败项重试；运行中 GUI 不得卡死。
- 显示当前 URL、总进度、路由结果、截图/附件进度与实时日志。
- 用户可查看每条记录的标题、作者主页 URL、状态码和错误信息，即使这些内容不进入固定模板。
- 所有抓取、截图、下载和模板处理默认在本机完成；不得上传用户 URL、内容、截图或 Excel。

## 8. 非功能需求

- 兼容性：Windows 10/11，Python 3.11 及以上；运行环境需具备可自动化的 Microsoft Excel，用于保持该固定模板的原生特性。
- 可靠性：默认并发 3，可配置 1 至 10；单条超时、429、403、重定向、解析失败或截图失败不得中断其余 URL。
- 可追溯性：每项运行态结果关联全局证据 ID、原始 URL、最终 URL、平台路由、页面/主页截图、状态码和错误信息。
- 安全与合规：不自动破解验证码，不隐匿自动化身份，不绕过付费墙、访问控制或账号权限；仅采集用户有权处理的信息。Cookie、令牌和登录态不得写入日志或 ZIP。
- 可维护性：平台域名识别、字段解析、模板映射、截图命名和附件校验应各自独立配置与测试。

## 9. 验收标准

1. 任务执行前后，源 `template/` 的文件清单和文件哈希完全一致。
2. 交付物固定为 `template.zip`；解压后仅包含 `template/` 根目录、`template.xlsx` 和被 Excel 引用的截图/附件文件。
3. 交付工作簿保留 8 个原始工作表、顺序、表头、下拉值、日期格式和工作表保护；不出现新增的状态码、错误信息或调试列。
4. 每条成功导出的 URL 根据模板允许枚举写入唯一正确工作表，所有 `发布平台`、`文本类型` 和电商处置对象均能通过 Excel 数据验证。
5. 取得标题或正文且主截图成功的记录均可导出；其他无法可靠获取的字段保持空白，不写入虚构内容。
6. 每个主截图列引用的文件均存在；可取得时，作者主页截图写入附件列且文件真实存在。每条记录最多两张截图。
7. 日期在 Excel 中为可排序的日期时间值，显示格式保持模板原有格式；页面标题、作者主页 URL、状态码、错误信息等运行态字段可在 GUI/本机日志中查询。
8. 未匹配平台、受登录限制页面、403/429、超时、空白页、截图失败、图片下载失败等情形不会中断整个批处理，并有可定位的错误记录。
9. 打开生成的 `template.xlsx` 时不得出现 Excel 修复提示；ZIP 内部文件名不得重名、不得包含无效路径，且不存在 Excel 中引用但缺失的附件。

## 10. 已发现且需确认的事项

1. 当前模板无“标题”（除电商/公众号）、“作者主页 URL”、“页面图片 URL 列表”、“HTTP 状态码”或“错误信息”列。本文将它们定义为运行态审计数据；若下游系统要求在 ZIP 中看到这些值，必须获得允许新增文件或修改模板的明确授权。
2. 当前基准示例有附件名引用但在 `template/` 中找不到对应文件，也有 `35.png` 与 `035主页.jpg` 的编号不一致。新输出应执行严格“引用文件必须存在”的校验，不继承这些不一致；如接收系统要求复刻这些例外，须提供正式命名规范。
3. 表头要求连字符日期格式，但实际单元格格式为斜杠日期格式。本文选择保留模板格式并写入原生日期值；如接收方要求文字形式的 `yyyy-mm-dd HH:mm:ss`，需要修订模板。
4. 已确认公众号微信号、图文视频/浏览器用户账号在公开页面无法稳定取得时，可用可靠昵称回退；运行态必须保留 `nickname_fallback` 审计标记，不能将其描述为平台真实 ID。
5. 群聊和朋友圈没有 URL 字段，且通常涉及非公开数据。它们是否需要后续的人工导入功能，应作为独立需求，不应混入网页 URL 爬虫范围。
6. 大部分社交平台对未登录浏览器、频率、地区或账号状态敏感。需要确认允许的 Cookie 导入、可视化登录、代理和重试策略，以及合法授权边界。
7. 当前模板视觉上主要预留到第 201 行，而电商表的已用区域更少。单次任务的最大记录数、溢出分包策略和是否允许扩展已格式化行需要在开发前确定。
8. 需确认 `template.zip` 是否要求保留顶层 `template/` 目录；本文按“压缩整个模板目录”的方式定义。如接收系统要求 ZIP 根目录直接放文件，应调整归档规则，但不能改变文件本身的布局。

## 11. 实施优先级

1. 先实现模板副本管理、Excel 原生写入验证、ZIP 结构校验和固定命名，建立不会损坏模板的交付链路。
2. 实现 URL 输入、平台识别、运行态结果模型和分表路由。
3. 实现页面解析、主截图、作者主页截图、页面图片下载与附件引用校验。
4. 实现 GUI、任务取消/重试、登录态配置和失败项人工补录。
5. 为每个工作表的字段映射、标准枚举、附件完整性、模板不变性、失败恢复和 ZIP 清单建立自动化测试。

## 12. 参考项目采纳原则

本项目参考 `references/` 中的三个项目，但不复制其业务边界或实现代码；仅吸收与固定模板批量取证场景相符的架构思想。最终实现以本需求文档和 `template/` 目录为准。

| 参考项目 | 采纳内容 | 不采纳内容 | 结论 |
| --- | --- | --- | --- |
| `MediaCrawler-main` | 异步任务生命周期、显式资源清理、浏览器上下文管理、平台适配器边界、可配置的登录态和失败恢复 | 代理池、反检测脚本、验证码/滑块处理、请求签名、默认连接用户浏览器、搜索型多存储工厂 | 作为爬取编排与资源管理的主要参考。 |
| `MediaCrawler-new-main` | 较小的代码边界、少量平台适配器的渐进扩展方式 | 模块级可变全局配置、平台专用数据模型直接贯穿全链路 | 作为“先做小、再扩展”的结构参考。 |
| `浏览器插件` | 以模板工作表、表头、示例行、数据验证和截图/附件关联作为交付契约；先校验再导出 | 以 SheetJS 新建工作簿、仅导出单工作表、浏览器下载逐文件输出 | 作为模板字段理解和附件关联的主要参考。 |

基于上述取舍，MVP 必须采用标准 Playwright 浏览器上下文。用户可显式导入自己的 `storage_state`/Cookie 或选择可视化模式完成合法登录，但系统不得默认连接或控制用户已打开的浏览器，不得执行反检测、绕过验证码、规避频率限制或使用未经用户授权的代理。

由于参考插件的导出实现会重建工作簿，不能保留本项目 `template.xlsx` 的受保护工作表、数据验证和原生格式，因此固定模板只能通过 Excel 原生自动化写入其副本。任何未来的“重建 Excel”方案均不满足本项目交付要求。
